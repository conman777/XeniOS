<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="jp.xenios.emulator">

    <uses-feature
        android:name="android.hardware.vulkan.level"
        android:required="true"
        android:version="0" />

    <uses-feature
        android:name="android.hardware.vulkan.version"
        android:required="true"
        android:version="0x400000" />

    <!-- Granted automatically - guest sockets. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light"
        tools:targetApi="s">

        <activity
            android:name="jp.xenios.emulator.LauncherActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="jp.xenios.emulator.GpuTraceViewerActivity"
            android:exported="false"
            android:label="@string/activity_label_gpu_trace_viewer"
            android:screenOrientation="sensorLandscape"
            android:theme="@android:style/Theme.Material.Light.NoActionBar" />

        <activity
            android:name="jp.xenios.emulator.EmulatorActivity"
            android:exported="false"
            android:label="@string/activity_label_emulator"
            android:screenOrientation="sensorLandscape"
            android:theme="@android:style/Theme.Material.Light.NoActionBar" />

        <activity
            android:name="jp.xenios.emulator.WindowDemoActivity"
            android:exported="false"
            android:label="@string/activity_label_window_demo"
            android:theme="@android:style/Theme.Material.Light.NoActionBar" />

    </application>

</manifest>
